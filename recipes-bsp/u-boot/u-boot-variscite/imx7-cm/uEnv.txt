findfdt=if test $fdt_file = undefined; then if test $som_rev = EMMC; then if test ${use_m4} = yes; then setenv fdt_file imx7d-cm-emmc-m4.dtb; else setenv fdt_file imx7d-cm-emmc.dtb; fi; fi; if test $som_rev = NAND; then if test ${use_m4} = yes; then setenv fdt_file imx7d-cm-nand-m4.dtb; else setenv fdt_file imx7d-cm-nand.dtb; fi; fi; if test $fdt_file = undefined; then echo WARNING: Could not determine dtb to use; fi; fi;
